<p id="notice"><%= notice %></p>
<div id="show">
  <%= render partial: 'logged_in', locals: {pin: @pin} %>
  <div id="title">
    <%= @pin.title %>
  </div>
  <div id="descrip">
    <% if @pin.link%>
    <%= link_to "#{@pin.link}", "#{@pin.link}" %>
    <% end %>
    </br>
    <%= @pin.description %>
  </div>
  <div class="container">
    <div class="image">
      <%= link_to (image_tag @pin.image.url), @pin %>
    </div>
  </div>
</div><br><br><br><br><br><br>

<div class= "container">
   <center> <%= link_to "Create Comment", new_pin_comment_path(:pin_id => @pin.id), "data-toggle" => "modal", 'data-target' => '.uniqueClass', :remote => true  %> </center>
  <% if !current_user.nil? %>
  <center>
  </div>


  <div id="commentModal">
    <div class="modal hide fade">
      <div class="modal-header">
        <button type="button" class="close" data-dismiss="modal" aria-hidden="true">&times;</button>
        <h3>Modal header</h3>
      </div>
      <div class="modal-body uniqueClass">
        <p>One fine body…</p>
      </div>
      <div class="modal-footer">
        <a href="#" class="btn">Close</a>
        <a href="#" class="btn btn-primary">Save changes</a>
      </div>
    </div>
  </div>
  </center>
  <% end %>
</div>
<br><br><br><br><br>
<div>
<script type="text/javascript" charset="utf-8">
$(".uniqueClass").html('<%= j render "comments/form" %>')
</script>

<script type="text/javascript" charset="utf-8">
  $(function() {
    $('form').submit(function(event) {
      event.preventDefault();
    });
  });

</script>
</div>

